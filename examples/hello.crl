#include <carlinho.h>

using namespace facilitador

int main() {
    string msg = "Hello from Carlos!"
    cout << msg << "\n"
    
    for (int i = 0, i < 3, i++) {
        cout << i << "\n"
    }
    
    return 0
}
